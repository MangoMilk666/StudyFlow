# StudyFlow (Docker Compose) 环境变量模板
#
# 用法：复制为根目录 `.env`（不要提交到仓库）。
# Docker Compose 会自动读取根目录 `.env`，并把变量注入到后端容器。

# Canvas LMS API
# 说明：用于从 Canvas 拉取课程与作业。
# 示例：CANVAS_BASE_URL=https://<your-school>.instructure.com
CANVAS_BASE_URL=

# 说明：Canvas 个人访问 Token（敏感信息，不要提交）。
CANVAS_TOKEN=
