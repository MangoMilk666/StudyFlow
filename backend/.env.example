## StudyFlow Backend 环境变量模板
##
## 说明：当前项目主要通过 Docker Compose 运行后端。
## - Docker Compose 运行时：优先在“根目录 .env”配置 `CANVAS_*`，并由 compose 注入容器。
## - 如需单独本地运行后端（非 Docker）：可在本文件配置 Mongo/JWT/Canvas。

PORT=8000

# MongoDB 连接串
MONGO_URI=mongodb://database:27017/studyflow

# JWT 签名密钥（敏感信息，不要提交）
JWT_SECRET=

NODE_ENV=development

# Canvas LMS API
CANVAS_BASE_URL=
CANVAS_TOKEN=

